name: release
on: [workflow_dispatch]
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup bun
        uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun run build
